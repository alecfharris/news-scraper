<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-beta.3/css/bootstrap.min.css">
  <title>News Scraper</title>
    <style>
      body { background-color: rgb(34, 34, 34) }
      #wrapper {
        width: 960px;
        margin-left: auto;
        margin-right: auto;
        padding-top: 5%;
      }
      #articles {
        display: inline-block;
        overflow: auto;
        width: 60%;
        height: 700px;
      }
      #notes {
        float: right;
        width: 30%;
      }
      textarea, input {
        display: block;
        width: 100%;
        background-color: rgb(51, 51, 51);
      }
      input {
        height: 36px;
        font-size: 24px;
      }
      textarea {
        height: 200px;
        font-size: 18px;
      }

      .delete {
        margin-left: 20px;
        color: red;
        cursor: pointer;
        text-align: right;
      }

      h1, h2, #articles, #notes, textarea, input {
        color: white;
      }

    </style>
</head>

<body>
  {{{body}}}
</body>

</html>